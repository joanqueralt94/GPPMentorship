#pragma once

#include <vector>
#include <algorithm>
#include <unordered_map>
#include "../utils/Log.h"

class TwoSumTarget
{
public:
	std::vector<int> twoSum(std::vector<int>& nums, int target);

	TwoSumTarget()
	{

	}
};

